# mini-swe-agent -> LiteLLM gateway. Copy to ~/.config/mini-swe-agent/.env
# .env holds the real key -- gitignored, do not commit.
#
# Three non-obvious keys make this "just work" against a LOCAL backend:
#   MSWEA_CONFIGURED=true          -> skip the first-run wizard, which aborts with no TTY
#                                     and only asks for cloud keys you do not need.
#   MSWEA_COST_TRACKING=ignore_errors -> LiteLLM reports $0 for local models; the default
#                                     mode treats $0 as an error and kills the run.
#   MSWEA_MINI_CONFIG_PATH=...      -> points at mini.yaml, which sets model_class
#                                     (there is no env var for model_class).
MSWEA_CONFIGURED=true
MSWEA_MODEL_NAME=openai/local-fast
MSWEA_COST_TRACKING=ignore_errors
OPENAI_API_BASE=http://127.0.0.1:4000/v1
OPENAI_API_KEY=__LITELLM_MASTER_KEY__
MSWEA_MINI_CONFIG_PATH=__HOME__/.config/mini-swe-agent/mini.yaml
